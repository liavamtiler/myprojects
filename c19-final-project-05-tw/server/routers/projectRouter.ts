import express from "express";
import { projectcontroller } from "../main";
export const projectRouters = express.Router();

//projectRouters.post('/projectDetail/:fid', projectcontroller.getproject)
// projectRouters.get('/projectDetail', projectcontroller.getproject)
